namespace LeetCode_20._Valid_Parentheses;public class Solution{    public bool IsValid(string s)    {        do        {            for (var i = 0; i < s.Length; i++)            {                if (i + 1 < s.Length)                    if (s[i + 1] == GetClosing(s[i]))                    {                        s = s.Remove(i, 2);                        break;                    }                if (i == s.Length - 1)                    return false;            }        } while (s.Length > 0);        return true;    }    private static char? GetClosing(char open)    {        return open switch        {            '(' => ')',            '{' => '}',            '[' => ']',            _ => null        };    }}